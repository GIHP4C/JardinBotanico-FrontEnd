import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jardin_botanico',
  appName: 'jardin_botanico',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
